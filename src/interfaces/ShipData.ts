export default interface ShipData {
    ship: {
        title: string;
        shields: string;
        hull: string;
        top_speed: number;
        acceleration: number;
        turn_speed: number;

    } 
};